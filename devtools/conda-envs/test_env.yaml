name: molscene-test
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pip
  - numpy
  - pandas
  - pdbfixer
  - lark
    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
  # Pip-only installs
  #- pip:
  #  - codecov

