name: molscene-test
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pip
  - numpy
  - pandas
    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
  # Pip-only installs
  #- pip:
  #  - codecov

